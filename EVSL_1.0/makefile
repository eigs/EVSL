ll:
	( cd SRC    ; make )
	( cd TESTS_Gen  ; mkdir -p OUT  ; make -f makefileP)
	( cd TESTS_Lap  ; mkdir -p OUT  ; make -f makefileP)
clean:
	( cd SRC    ; make clean)
	( cd TESTS_Gen  ; make -f makefileP clean)
	( cd TESTS_Lap  ; make -f makefileP clean)

